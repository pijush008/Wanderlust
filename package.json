{
  "name": "wanderlust",
  "version": "1.0.0",
  "description": "Wanderlust — Travel Booking Platform (Frontend + Backend)",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently -n BACKEND,FRONTEND -c yellow,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "concurrently -n BACKEND,FRONTEND -c yellow,cyan \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "seed": "cd backend && node init/index.js && node init/seedFilters.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
